name: Sole Next 16 Lock Sync

on:
  push:
    branches:
      - chatgpt/sole-live-duplicate-20260912

permissions:
  contents: write

jobs:
  sync-lock:
    if: ${{ !contains(github.event.head_commit.message, '[lock-sync]') }}
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: chatgpt/sole-live-duplicate-20260912
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - name: Refresh patched dependency graph
        run: npm install --save-exact next@16.3.5 react@19.1.1 react-dom@19.1.1 && npm install --save-dev --save-exact eslint@9.35.0 eslint-config-next@16.3.5
      - name: Commit regenerated lockfile
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package.json package-lock.json
          if git diff --cached --quiet; then
            echo "Dependency graph already synchronized."
            exit 0
          fi
          git commit -m "Sync Sole Exchange Next 16 dependency graph [lock-sync]"
          git push origin HEAD:chatgpt/sole-live-duplicate-20260912

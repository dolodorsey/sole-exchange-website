import './globals.css';

export const metadata = {
  title: 'The Sole Exchange — Air Force One’s Drive',
  description: 'Give sneakers. Request support. Volunteer, partner, and help move dignity forward through The Sole Exchange.',
};

export default function Layout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}

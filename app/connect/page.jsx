'use client';
import {useState} from 'react';
import Link from 'next/link';
export default function Connect(){const[status,setStatus]=useState('');async function submit(e){e.preventDefault();setStatus('Sending…');const body=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/inquiry',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});const data=await r.json();if(r.ok){e.currentTarget.reset();setStatus('Thank you. Your request was received and will be routed for review.')}else setStatus(data.error||'The request could not be sent. Please try again.')}return <main><header className="header"><Link href="/"><img src="/brand/sole-logo.png" alt="The Sole Exchange"/></Link><nav><Link href="/give">Give</Link><Link href="/request">Request</Link><Link href="/volunteer">Volunteer</Link><Link href="/partners">Partner</Link></nav></header><section className="formWrap"><form className="form" onSubmit={submit}><h1>Choose your path.</h1><p>Share only the information needed to route your request. Submission does not guarantee inventory, approval, placement, or partnership.</p><label>Request type<select name="submission_type" required><option value="">Choose one</option><option value="donate">Donate sneakers</option><option value="request">Request support</option><option value="volunteer">Volunteer</option><option value="partner">Partner or host</option><option value="contact">General question</option></select></label><label>Full name<input name="full_name" required minLength="2" autoComplete="name"/></label><label>Email<input name="email" type="email" required autoComplete="email"/></label><label>Phone<input name="phone" type="tel" autoComplete="tel"/></label><label>Organization<input name="organization" autoComplete="organization"/></label><label>City<input name="city" required autoComplete="address-level2"/></label><label>Details<textarea name="details" rows="7" required maxLength="4000" placeholder="Sizes, quantity, timing, role, contribution, or the support needed."/></label><button type="submit">Send request</button>{status&&<div className="notice" role="status">{status}</div>}</form></section></main>}

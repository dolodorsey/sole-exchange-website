import {NextResponse} from 'next/server';
export async function POST(request){try{const body=await request.json();if(!body.full_name||!body.email||!body.city||!body.details||!body.submission_type)return NextResponse.json({error:'Please complete every required field.'},{status:400});const hook=process.env.N8N_INTAKE_WEBHOOK;if(!hook)return NextResponse.json({error:'The intake connection is temporarily unavailable.'},{status:503});const response=await fetch(hook,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({...body,brand:'Sole Exchange',source:'sole-exchange-website',submitted_at:new Date().toISOString()})});if(!response.ok)throw new Error('Intake service rejected the request.');return NextResponse.json({ok:true})}catch(error){return NextResponse.json({error:'The request could not be sent. Please try again.'},{status:500})}}
